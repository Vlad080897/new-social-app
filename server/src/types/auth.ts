export type LoginCredentials = {
  email: string;
  password: string;
};

export type CredentialsType = {
  first_name: string;
  last_name: string;
  username: string;
  password: string;
  email: string;
};
